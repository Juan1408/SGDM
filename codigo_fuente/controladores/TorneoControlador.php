<?php
/* TorneoControlador.php
 * Controlador para crear, editar, listar y ver torneos.
 * - recibir datos del formulario
 * - invocar servicios de torneo
 * - devolver vistas: listado, detalle, crear
 */

class TorneoControlador extends ControladorBase {
    // Implementar métodos: index(), crear(), editar(), ver()
}
